import styled from '@emotion/styled';
import { Link } from 'react-router-dom';

export const EmptyCardsText = styled.h2`
  text-align: center;
`;

export const LinkStyled = styled(Link)`
  text-align: center;
  color: var(--brand-color);
  text-decoration: underline;
  line-height: 20px;
`;
